/**
 * DbSummary usunięty — totale i rozmiar bazy przeniesione do CollectorStatus
 * (endpoint /api/health/stats). Komponent zachowany jako pusty placeholder
 * żeby nie łamać importu w App.tsx.
 */
export default function DbSummary() {
  return null;
}
