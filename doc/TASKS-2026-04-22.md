# Tasks — 2026-04-22 (post-prod-logs audit)

> **Każdy task = osobna sesja Claude Code.** Self-contained context, niezależny scope. NIE ładować więcej niż 1 task w sesji.

## Źródła

- Audyt logów produkcji 22.04.2026 (system_logs export, 500 wpisów, 18h okno)
- Sprint 18 candidates z CLAUDE.md (V5 backtest follow-ups)
- Phase 2-3 bugi z [STOCKPULSE-AUDIT-2026-04-16.md](STOCKPULSE-AUDIT-2026-04-16.md)

---

## Kolejność rekomendowana (priorytet × zależności)

### Sesja 1 — P0 obserwowalność (musi być pierwsze, blokuje diagnostykę kolejnych)

- [x] **TASK-01** ✅ DONE 22.04.2026 commit `6191a81` — AlertDispatcherService + @Logged
  - Nowy serwis `src/alerts/alert-dispatcher.service.ts` z metodą `dispatch()` `@Logged('alerts')`
  - Refactor 5 konsumentów (Form4, Form8k×2, Correlation, AlertEvaluator, OptionsFlow) — ~200 LoC duplikacji usunięte
  - Priority order suppression: observation > sell_no_edge > csuite_sell > cluster_sell > silent > daily_limit
  - Test jednostkowy 10 cases w `test/unit/alert-dispatcher.spec.ts`
  - **Odblokowuje**: TASK-04, TASK-05, TASK-07 (każdy alert teraz widoczny w system_logs z module=alerts, function=dispatch, decisionReason=ALERT_*)

### Sesja 2 — P0 logiczne bugi (niezależne od siebie, ta sama warstwa)

- [x] **TASK-02** ✅ DONE 22.04.2026 — Form4Pipeline hard skip dla non-role SELL
  - Decision tree krok 4b w `Form4Pipeline.onInsiderTrade` PRZED daily cap + observation gate
  - `transactionType==SELL AND !isCsuiteRole AND !isDirectorRole → SKIP_NON_ROLE_SELL`
  - Nowa eksportowana `isDirectorRole()` (parallel do `isCsuiteRole`)
  - Decision tree pełny komentarz inline (8 kroków)
  - 29 testów (14 isDirectorRole + 15 SKIP_NON_ROLE_SELL z ASX Chen Tien-Szu case)
  - **Diagnosis**: `isCsuiteRole("GM, ASE Inc. Chung-Li Branch")` zwracał false (whitelist OK).
    Problem był w kolejności decision tree — observation gate (semi ticker) odpalał ZANIM
    filter ról zdążył odrzucić ten case → fałszywy correlation alert przez observation route

- [x] **TASK-03** ✅ DONE 22.04.2026 — multi-transaction Form 4 aggregation (collector-level)
  - Fix w `sec-edgar.service.ts parseAndSaveForm4`: save all rows + groupBy (insiderName, transactionType, is10b51Plan) w obrębie filing → emit 1 event per grupa z aggregate values + aggregateCount + aggregateTradeIds
  - Pipeline przekazuje aggregate do GPT prompt (nowy pole `aggregateCount` w `Form4PromptData`, buildForm4Prompt dodaje note "Split-fill execution")
  - Throttle przestaje być problemem bo 1 event per filing (zamiast N)
  - N× oszczędność GPT daily cap dla multi-transaction Form 4
  - 12 nowych testów w `test/unit/form4-aggregation.spec.ts` (ASX case, BUY+SELL split, plan+discretionary split, primary ordering, prompt rendering)
  - **Odrzucony alternative design** z pseudokodu: 60s time window + `updateSignalConviction` (race condition: trade 1 event fires przed save trades 2-4). Collector-level aggregation gwarantuje atomic grouping bez race.

### Sesja 3 — P1 spam alertów

- [x] **TASK-04** ✅ DONE 22.04.2026 — CorrelationService pattern detection deduplication (content hash)
  - Content-hash dedup w `runPatternDetection`: in-memory `Map<ticker, {hash, ts}>` z 15-min window
  - Hash = sha256(sorted (type + signalIds)) truncated 16 chars — exported `hashPatternSet()` + `shouldSkipDuplicatePatternDetection()` jako czyste funkcje
  - Nowy action: `PATTERNS_DETECTED_DUPLICATE` (zamiast `PATTERNS_DETECTED`) gdy skład nie zmienił się
  - `@Logged('correlation')` na `triggerCorrelatedAlert` — zamyka gap audytu (wcześniej DEDUP_SKIP / SKIP_LOW_CONVICTION / dispatch actions niewidoczne w system_logs)
  - 20 nowych testów (hash stability, order-insensitivity, window boundary, HPE cascade scenario)
  - **Diagnoza zmieniła scope**: tylko 1 Telegram alert wyszedł z HPE cascade (PATTERN_THROTTLE 2h już działał). 7 PATTERNS_DETECTED = CPU + log clutter, nie alert spam. Propozycja miała kilka założeń niezgodnych z kodem (TTL cleanup już jest via ZREMRANGEBYSCORE, `Pattern.contributingSignalIds` już jest jako `signals[]`).

- [x] **TASK-05** ✅ DONE 22.04.2026 — observation visual distinction w portalu (Opcja A)
  - **Re-scope po investigation**: backend gate DZIAŁA (ASX DB: `delivered=false, nonDeliveryReason='observation'`, Telegram suppressed via AlertDispatcher). Gap był **tylko frontend** — portal renderował observation alerty jako CRITICAL.
  - Backend: `alerts.controller.ts` — dodane `delivered + nonDeliveryReason` w `/outcomes` response, w `/timeline` + `/recent-timeline` SQL SELECT. Endpoint `/alerts` już zwracał pełny entity.
  - Frontend: `Alert` interface dostało `nonDeliveryReason`, `PriorityChip` wyciszony (color default + variant outlined + label `"${priority} (${reason-label})"`) gdy alert ma `nonDeliveryReason !== null`. `SignalTimeline` PRIORITY cell podobnie.
  - **Odrzucono Opcja B** (conviction cap -0.30): maskowałaby magnitude w DB, co utrudniłoby retrospective backtest H2 (Python re-run w TASK-11).

### Sesja 4 — P1 health checks (niezależne, można parallelnie)

- [x] **TASK-06** ✅ DONE 23.04.2026 — PDUFA collector parser observability (Opcja A)
  - **Diagnoza**: scraper DZIAŁA. Parser returns 14 events (pdufa.bio 2026 aktualna tabela), wszystkie 14 w DB → dedup → `newCount=0`. Dodatkowo 5 stale rows z marca (scraped_at 2026-03-02) to PDUFA dates z przeszłości usunięte z pdufa.bio — pozostają w DB bez update. `scraped_at` istniejących eventów update'owany dziś 08:26. SEC EDGAR też ma dni z count=0 (sobota + niedziela + pre-NYSE) — też legit.
  - **Re-scope**: task zakłada alarm "3+ consecutive zeros" — odrzucone, PDUFA ma legit okres 2+ tygodnie bez nowych dat (false positive 90% roku). Prawdziwy sygnał awarii to `parsed.length === 0` (strona zmieniła strukturę, HTTP OK ale 0 rows).
  - **Fix**: `PdufaBioService.scrapeAndInsert()` zwraca `{inserted, parsed}`. Override `runCollectionCycle()` z `@Logged('collectors')` — output `{collector, count, parsed, action?}`. Gdy parsed===0, action='PARSER_EMPTY'. Dodany mapping `PARSER_EMPTY → level='warn'` w `logged.decorator.ts extractLogMeta()`.
  - **Widoczność**: System Logs tab pokazuje `{count: 0, parsed: 14}` dla dedup cycles (normalny flow) vs `{count: 0, parsed: 0, action: 'PARSER_EMPTY'}` z warn chip (alarm). Rozróżnia "nic nowego" od "scraper broken".
  - 8 testów w `test/unit/pdufa-collector.spec.ts`: parser empty detection (3), warn level contract (1), runCollectionCycle integration z mocked fetchPage+repo (4 — dedup, empty, new insert, HTTP throw).

- [x] **TASK-07** ✅ DONE 23.04.2026 — AlertDeliveryGate stats audit (scenariusz C)
  - **Diagnoza 48h (12 alertów)**: 11 observation (semi HPE×4, WDC, ASX×2, DELL, ONTO + GILD/HIMS oznaczeni jako obs) + 2 sell_no_edge (HIMS Form 4 Sprint 17 route) + 2 delivered (HIMS, DXCM Correlated). **0 daily_limit hits, 0 telegram_failed, 0 csuite_sell**. Limit `AlertDeliveryGate.MAX_TELEGRAM_ALERTS_PER_SYMBOL_PER_DAY=5` hardcoded (nie env), per-ticker, nie konsumowany przez raporty 8h (`SummarySchedulerService.sendSummary` pomija `alertRepo.save`).
  - **Wniosek**: system działa poprawnie — "Łącznie: 2 (dostarczono: 0)" w raporcie 8h to legit observation, nie bug. Scenariusz A (gate bug) i B (limit za niski) odrzucone.
  - **Fix (scenariusz C, UX)**: `SummarySchedulerService.sendSummary` dodaje drugi query `GROUP BY a.nonDeliveryReason FILTER (WHERE delivered=false AND nonDeliveryReason IS NOT NULL)`; `formatSummary` renderuje sekcję _Niedostarczone (N):_ z PL etykietami z static `REASON_LABELS` (observation → "Obserwacja", sell_no_edge → "SELL (zero edge)", csuite_sell_no_edge → "C-suite SELL (zero edge)", cluster_sell_no_edge → "Cluster SELL (zero edge)", silent_rule, silent_hour, daily_limit → "Dzienny limit", telegram_failed).
  - **Tests**: `test/unit/alert-delivery-gate.spec.ts` (6 cases — under/at/over limit, UTC todayStart filter, static constant) + `test/unit/summary-scheduler.spec.ts` (5 cases — scenario C replay, no-suppressed, zero-alerts, unknown reason fallback, all 7 reason labels).
  - **Odrzucone z task description**: raport 8h jako self-consumer limit (`sendSummary` nie zapisuje do alerts), hardcoded limit bump (5/ticker/dzień realistic dla 42-ticker universe), scenariusz A/B fix (brak bug'u).

- [ ] **TASK-08** — ~~SEC EDGAR collector AbortSignal.timeout (FLAG #28) (30min)~~ **✅ DONE 22.04.2026** commit `7e63d9d` (AbortSignal.timeout 30s na fetchUrl + fetchText, analogicznie do Options Flow d78a92f). Zamknięte w CLAUDE.md → Already resolved.

### Sesja 5 — Sprint 18 V5 follow-ups (Python backtest, brak kontaktu z produkcją)

- [x] **TASK-09** ✅ DONE 23.04.2026 — INSIDER_CLUSTER BUY disable (Opcja A)
  - **Decyzja Przemka**: Opcja A (hard disable na poziomie detekcji). Opcja B (hybrid boost retroactive na solo BUY alert) odłożona na Sprint 19.
  - **Diagnoza**: stare `triggerCorrelatedAlert:544-545` comment "BUY clusters d=0.47 p=0.009 nadal alertują normalnie" był V4, obalony przez V5 f69cfa8 (cluster_buy_vs_single_buy N=21/49, p=0.445/0.700/0.955/0.371 dla 1d/3d/7d/30d). Solo BUY rule (Form 4 Insider BUY) pokrywa signal: V5 C-suite BUY d=+0.92 (7d), All BUY d=+0.75.
  - **Historic audit**: 90d DB: 0 INSIDER_CLUSTER BUY alertów (wszystkie 8 to SELL observation), więc retroactive archive niepotrzebny.
  - **Fix**: `detectInsiderCluster` w `correlation.service.ts` zwraca `null` gdy `dir==='positive'` (BUY). SELL zostaje (observation mode w `triggerCorrelatedAlert`, Sprint 15). Stale V4 comment przepisany — flaga `isClusterSellObservation` zostaje jako defence-in-depth.
  - **Testy**: 6 nowych cases w `test/unit/correlation.spec.ts` TASK-09 block (2 BUY / 3 BUY → null, 2 SELL → emit, mixed 1/1 → null via getDominantDirection, >7d window → null, non-form4 social BUY → null). 296/296 pass.
  - **Odrzucone**: Opcja B hybrid boost (Sprint 19 — większy task, wymaga alert retroactive mutation), Opcja C no-op (V5 N=21 mała próbka ale p>0.37 nie budzi wiary w edge).

- [x] **TASK-10** ✅ DONE 23.04.2026 — C-suite regex unification
  - **Problem**: 2 miejsca w `form4.pipeline.ts` definiowały C-suite: linia 170 inline regex `/\bCEO|CFO|COO|CTO|President|Chair|Chief\b/i` (catch-all, używany w `isPureDirector` detection) vs linia 325 `isCsuiteRole()` (explicit whitelist w boost + step 4 SELL gate). Niespójne semantyki: broad regex łapał "Vice President" (no negative lookbehind), Chief Marketing/Communications/Sustainability (Sprint 16b soft roles explicit wyłączone z whitelist).
  - **Fix**: linia 170 `hasCsuite = isCsuiteRole(role)`. `C_SUITE_PATTERNS` rozszerzone o `/\bChair(man|woman|person)\b/i` i `/\bVice\s+Chair(man|woman|person)\b/i` — gender-neutral warianty, które stary broad regex też łapał (`\bChair` prefix match). Bez rozszerzenia `Chairwoman`/`Chairperson` ciche regresji w step 3. Docstrings zaktualizowane (kroki 3/4/7 decision tree identyczna definicja C-suite).
  - **Skutki semantyczne** (preserved + new):
    - bez zmiany: Director + CEO/CFO/Chairman/President/CMO → `hasCsuite=true` → NIE pure Director → proceed to GPT (zgodnie z Sprint 16 FLAG #30).
    - change: Director + Vice President — stary bug łapał `\bPresident\b` bez lookbehind, nowy strict isCsuiteRole z lookbehind. Teraz pure Director SELL → `SKIP_DIRECTOR_SELL`.
    - change: Director + Chief Marketing/Communications/People/Sustainability Officer — stary `\bChief\b` catch-all. Teraz pure Director SELL → `SKIP_DIRECTOR_SELL` (zgodnie z Sprint 16b decyzją że soft roles nie mają insider info).
    - change: Director + Executive Vice President (bez finance/ops context) — analogicznie, teraz pure Director SELL → SKIP.
  - **Testy**: 26 nowych w `test/unit/form4-pipeline-rules.spec.ts` (322/322 pass, 13 suites): 6 cases Chair variants (Chairwoman, Chairperson, Vice Chairwoman, Vice Chairperson, Chairperson of the Board, "Chairing the Committee" → false), 20 cases isPureDirector co-filing (CEO/CFO/Chairman/CMO → NIE pure, VP/SVP/EVP bez context → pure, CMO Marketing/Communications/People/Sustainability → pure, pure Director/Independent Director, non-Director edge cases). `tsc --noEmit` clean.
  - **Odrzucone**: shared utility `src/common/utils/insider-roles.ts` (Form8kPipeline, AlertEvaluator nie mają C-suite logic — zbędne w tej chwili), Chair alone w whitelist ("Chairing the Committee" → false explicit — formalne SEC filings używają Chairman/Chairperson z suffix).

- [ ] **TASK-11** — d=None bug w `_direct_cluster_vs_single` (Python) (1h)
  - **Problem**: `scripts/backtest/V5/analyzer.py::_direct_cluster_vs_single()` zapisuje `None` zamiast wyliczonych Cohen's d w niektórych horizontach (JSON diff pokazuje). Weryfikować via `backtest_results.json` strukturę.

- [ ] **TASK-12** — report_generator render `hc_vs_ctrl_direct` + `cluster_buy_vs_single_buy` (1h)
  - **Problem**: JSON zawiera sekcje ale markdown report nie renderuje (`report_generator.py` nie obsługuje kluczy).
  - **Fix**: dodać obsługę nowych sub_groups w generatorze.

---

## Mapa zależności

```
TASK-01 (logging) ──┬─→ daje wgląd w TASK-04, TASK-05, TASK-07
                    │
TASK-02 (role filter) ──→ może zmienić co jest "throttled" w TASK-03
TASK-03 (magnitude) ──── niezależny
TASK-04 (dedup) ──────── niezależny od TASK-05 (różne miejsca)
TASK-05 (obs gate) ───── niezależny
TASK-06, 07, 08 ──────── całkowicie niezależne (TASK-08 = DONE)
TASK-09 ──────────────── decyzja produktowa (disable vs czekać)
TASK-10 ──────────────── niezależny
TASK-11, 12 ──────────── Python only, nie dotyka NestJS
```

## Co NIE jest na liście (świadomie)

- Nowe źródła danych (ClinicalTrials.gov, Polymarket) — prohibited do Sprint 18 ideation
- BUY threshold replication ($750K / $1.5M / $2M) — wymaga Python re-runa po TASK-11/12
- XBI-adjusted alpha — wymaga zewnętrznych danych benchmarku, większy task
- 8-K SKIP_LOW_PRIORITY rate audit — sample za mały (1 dzień), poczekaj tydzień

## Definicja "DONE" dla każdego taska

- Konkretne commity / zmiany w kodzie
- Testy jednostkowe (gdzie applicable)
- Update `CLAUDE.md` / `PROGRESS-STATUS.md` (jeśli rozwiązują flag/bug)
- Restart docker compose + weryfikacja w logach (gdzie applicable — tylko NestJS tasks)

## Status — 2026-04-22 21:XX UTC

- ✅ **TASK-08** DONE (commit `7e63d9d`, docs `53b94ef`)
- ✅ **TASK-01** DONE (commit `6191a81`, docs w CLAUDE.md Already resolved + PROGRESS-STATUS.md)
- ✅ **TASK-02** DONE 22.04.2026 — SKIP_NON_ROLE_SELL hard skip + isDirectorRole + decision tree komentarz + 29 testów
- ✅ **TASK-03** DONE 22.04.2026 — multi-transaction Form 4 aggregation (collector-level groupBy + aggregate event) + 12 testów
- ✅ **TASK-04** DONE 22.04.2026 — pattern content-hash dedup + `@Logged` na triggerCorrelatedAlert + 20 testów
- ✅ **TASK-05** DONE 22.04.2026 — observation visual distinction w portalu (Opcja A)
- ⏸ **6 pozostałych tasków** czeka na osobne sesje

**Szacunek łączny**: ~8h pracy Claude Code rozdzielonej na 2-3 pozostałe sesje. Rekomendowana kolejność następnej: S4 (06 PDUFA health probe + 07 AlertDeliveryGate stats) → S5 (09+10+11+12).

## Cleanup / regresje (naprawione przy okazji)

- ✅ **Stale AlertEvaluator specs** (commit `1dc3b16`, 22.04.2026): Usunięte describe blocks referencujące metody usunięte w FinBERT cleanup (`onSentimentScored`, `checkHighConviction`, `checkSentimentCrash`, `checkStrongFinbert`). Dodatkowo `createService()` dostał `createMockDispatcher()` jako 8-my parametr (TASK-01 dodał `AlertDispatcherService` do konstruktora). 271 testów pass.
