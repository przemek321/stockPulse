# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Dokumentacja i komentarze w tym projekcie piszemy po polsku.**

> 🎯 **Backtest V5 (18.04.2026, po Sprint 17 P1 — H6 control fix + H1 cluster-vs-single)** — V5 commit f69cfa8 (regenerate po mismatch w 3a319d7). **Healthcare SELL: zero edge** (d≈0 wszystkie horyzonty). **Control SELL: d=+0.10 30d Bonferroni ✓✓✓** (non-healthcare ma edge na dłuższym horyzoncie). **Direct HC vs CTRL: d=-0.14 30d p=0.016** — healthcare SŁABSZY niż control dla SELL. **H1 cluster BUY vs solo BUY: p>0.37 wszystkie horyzonty** (N_cluster=21 vs N_single=49) — czekanie na 2-giego insidera nie dodaje edge'u. **BUY edge silniejszy V4→V5** (C-suite BUY csuite_buys 7d: +0.82→+0.92, All BUY: +0.68→+0.75). Wyniki: [backtest_report.md](scripts/backtest/data/results/backtest_report.md), ściąga: [STOCKPULSE-CHEATSHEET-2026-04-17.md](doc/STOCKPULSE-CHEATSHEET-2026-04-17.md).
>
> ✅ **Sprint 17 resolved (18.04.2026)**: (1) C-suite SELL → observation mode (abff1c9, 5dc2a36). (2) Director BUY boost ×1.15 (e07bbc2). (3) H6 control group fix (e07bbc2). (4) H1 cluster_buy_vs_single_buy direct test (e07bbc2 + f69cfa8). (5) Production 10b5-1 parser audit verified. Sprint 18 **wszystkie 12 tasków done** (TASK-01..12 23.04.2026): ~~INSIDER_CLUSTER BUY disable~~ ✅ TASK-09, ~~C-suite regex ujednolicenie~~ ✅ TASK-10, ~~d=None bug w `_direct_cluster_vs_single`~~ ✅ TASK-11 (stale doc entry), ~~report_generator renderowanie nowych sub_groups~~ ✅ TASK-12.
>
> 🔧 **Audyt 16.04.2026**: Phase 1 (5 bugów P0-P2) + Tier 1 observability (traceId, level, ticker, decision_reason w system_logs) + 6 P0 fixów z code review (multi-owner parser, backfill contamination, baseline winsorize, bankruptcy cap, NYSE holidays, AlertDeliveryGate). Raport: [STOCKPULSE-AUDIT-2026-04-16.md](doc/STOCKPULSE-AUDIT-2026-04-16.md), handoff: [HANDOFF-CODE-REVIEW-2026-04-16.md](doc/HANDOFF-CODE-REVIEW-2026-04-16.md).

## Opis projektu

StockPulse to system wykrywania edge'u na rynku akcji healthcare z alertami Telegram. Monitoruje transakcje insiderów (SEC EDGAR Form 4), zdarzenia korporacyjne (8-K), aktywność opcyjną (Polygon.io) i daty FDA (PDUFA.bio). Koreluje sygnały z wielu źródeł (insider×options, insider×8-K, insider cluster) i alertuje tylko gdy wykryje realny edge.

**Aktualny stan projektu**: Faza 2 + Sprint 4-10 (ukończone) + **Sprint 11 (przebudowa — focus na edge, 03.04.2026)** + **Sprint 12 (migracja AI + dashboard, 04.04.2026)** + **Sprint 13 (Signal Timeline, 05.04.2026)** + **Sprint 14 (TickerProfile + Słownik, 05.04.2026)** + **Sprint 15 (Backtest + BUY rule, 06.04.2026)** + **Sprint 16 (UTC fix + Options Flow UX, 06.04.2026)** + **Sprint 17 (Semi Supply Chain — observation layer, 09.04.2026)**. Analiza 2 tygodni (962 alertów, 55.5% hit rate) wykazała brak edge'u na sentymencie. Sprint 11: wyłączenie szumu, focus na insider pipeline. Sprint 12: migracja z Azure OpenAI gpt-4o-mini na **Anthropic Claude Sonnet** (bezpośrednio z NestJS, bez Azure VM pośrednika), panel Status Systemu na dashboardzie, fix parsowania 8-K (inline XBRL), hard delete 1585 alertów z wyłączonych reguł. **Sprint 15**: backtest 3 lat danych SEC EDGAR (43 946 transakcji, 61 tickerów), 6 hipotez z Welch's t-test + Cohen's d. Wynik: **insider BUY to jedyny silny edge** (d=0.43, p<0.001, potwierdzone vs dip baseline). Nowa reguła "Form 4 Insider BUY" (min $100K, C-suite ×1.3, healthcare ×1.2). Director SELL = hard skip (anty-sygnał, 68% cena rośnie). INSIDER_CLUSTER SELL → observation mode (DB only, no Telegram). **Sprint 17**: Semi Supply Chain — 14 nowych tickerów (MU, WDC, STX, KLIC, AMKR, ONTO, CAMT, NVMI, ASX, DELL, HPQ, HPE, SMCI, NTAP) w **observation mode** (DB only, brak Telegram). Ticker entity: kolumny `sector` + `observationOnly`. Alert entity: `nonDeliveryReason` (observation/silent_hour/daily_limit). Healthcare boost guard: `sector === 'healthcare'`. Plan: [doc/plan-semi-supply-chain.md](doc/plan-semi-supply-chain.md). **Aktywne kolektory**: SEC EDGAR (Form 4 + 8-K), Options Flow (Polygon EOD), PDUFA.bio. **Wyłączone**: StockTwits (0% edge), Finnhub news (HFT lag), sentiment pipeline (usunięty 22.04.2026 wraz z FinBERT sidecarem). **8 aktywnych reguł**: Form 4 Insider Signal, **Form 4 Insider BUY** (Sprint 15), 8-K Material Event GPT, 8-K Earnings Miss, 8-K Leadership Change, 8-K Bankruptcy, Correlated Signal, Unusual Options Activity (tylko z PDUFA boost). **12 wyłączonych reguł** (isActive=false w DB). **3 aktywne wzorce korelacji**: INSIDER_CLUSTER (2+ discretionary C-suite w 7d, **SELL = observation mode**, **BUY = disabled TASK-09 23.04.2026 bo V5 cluster vs solo p>0.37**), INSIDER_PLUS_8K (insider + 8-K w 24h), INSIDER_PLUS_OPTIONS (insider + opcje w 120h/5d). **Wyłączone wzorce**: FILING_CONFIRMS_NEWS, MULTI_SOURCE_CONVERGENCE, ESCALATING_SIGNAL (wymagały sentymentu). Form4Pipeline: filtr **discretionary only** (is10b51Plan=true → skip), **Director SELL → hard skip** (Sprint 15, backtest: anty-sygnał), **C-suite priorytet** (CEO/CFO/President/Chairman/EVP → boost priority), **BUY boosty** (C-suite ×1.3, healthcare ×1.2 — backtest-backed). **Observation mode gate** w Form4Pipeline, Form8kPipeline, AlertEvaluator, CorrelationService, OptionsFlowAlertService — tickery z `observationOnly=true` zapisują alert do DB z `delivered=false`, `nonDeliveryReason='observation'`, brak Telegramu. Pokrywa **wszystkie 5 ścieżek** wysyłki Telegram. Options scoring: **spike ratio > 1000 → suspicious, conviction ×0.5**. Options Flow API: filtr `expiry >= today` (wygasłe opcje ukryte). Options Flow CRON: `tz: 'UTC'` explicite (BullMQ). `getLastTradingDay()`: `getUTCDay()`/`setUTCDate()` (fix: serwer Europe/Warsaw powodował błędny dzień handlowy). 8-K Item 5.02 prompt: rozróżnienie **voluntary+successor vs crisis vs relief rally**. **priceAtAlert** naprawiony dla Correlated Signal, Form 4, 8-K (wcześniej NULL). SEC filingi: Claude Sonnet z per-typ promptami, Zod walidacja, Item 1.03 Bankruptcy → natychmiastowy CRITICAL. CorrelationService: Redis Sorted Sets, progi MIN_CONVICTION=0.05, MIN_CORRELATED_CONVICTION=0.20. Price Outcome Tracker: CRON co 1h (NYSE open), hard timeout 7d. System Logowania: @Logged() na ~15 metodach z `extractLogMeta()`, tiered cleanup (debug 2d / info 7d / warn+error 30d) o 03:00 UTC (`@Cron('0 3 * * *', { timeZone: 'UTC' })`), traceId propagacja. **Backtest**: `scripts/backtest/` — 6 hipotez na 3 latach SEC EDGAR Form 4, wyniki w `scripts/backtest/data/results/backtest_summary.md`. **Sprint 16 (10.04.2026, w toku — walidacja Sprint 15)**: P0.5 fix backtest-production mismatch — zawężenie do 28 healthcare overlap + soft delete 9 production-only tickerów (ALHC, CERT, CVS, CYH, DVA, GSK, HCAT, VEEV, WBA). Soft delete dla alertów: `alerts.archived` column + endpoint `POST /api/alerts/archive`. Wyniki walidacji w bloku 🎯 na górze pliku. **Monitorowane tickery**: 42 aktywne — **28 zwalidowanych healthcare + 14 semi supply chain (observation mode)** + 9 soft-deleted (`isActive=false`). Config: [doc/stockpulse-healthcare-universe.json](doc/stockpulse-healthcare-universe.json) + [doc/stockpulse-semi-supply-chain.json](doc/stockpulse-semi-supply-chain.json). Cel: **3-5 alertów/tydzień z realnym edge** (insider BUY, insider sell healthcare, PDUFA options, korelacje). **Sprint 16b interim (17.04.2026)**: C-suite whitelist (explicit, bez soft roles typu Comm/People/Diversity), C-suite SELL → observation mode (5dc2a36, V4 d=-0.002), dead handler usunięty (98b3741), Options Flow timeout 30s (d78a92f). **Sprint 17 P1 V5 backtest (18.04.2026)**: Director BUY boost ×1.15 (e07bbc2, V4 d=+0.59), H6 control group fix (e07bbc2 — usunięty top-level `is_healthcare==True` filter z `run_analysis`), H1 cluster_buy_vs_single_buy direct test (e07bbc2 + f69cfa8 regenerate po 3a319d7 mismatch). V5 wyniki: healthcare SELL zero edge, control SELL d=+0.10 30d Bonferroni ✓✓✓, cluster vs solo BUY p>0.37 wszystkie horyzonty. Ściąga backtest: [STOCKPULSE-CHEATSHEET-2026-04-17.md](doc/STOCKPULSE-CHEATSHEET-2026-04-17.md). Raporty tygodniowe: [doc/reports/](doc/reports/). Szczegółowy status: [doc/PROGRESS-STATUS.md](doc/PROGRESS-STATUS.md). **22.04.2026 FinBERT cleanup**: usunięty sidecar + sentiment pipeline + entities + 3 endpointy REST (szczegóły w Already resolved, ~2400 LoC).

## Komendy (Makefile — autodetekcja środowiska)

```bash
# Makefile automatycznie wykrywa: jetson (aarch64) / gpu (x86+nvidia) / cpu (x86)
make up              # start całego stacku
make down            # stop
make rebuild         # rebuild po zmianach kodu
make rebuild-app     # rebuild tylko backendu NestJS
make status          # status kontenerów + health check
make logs            # logi (follow)
make log S=app       # logi konkretnego serwisu
make seed            # seed bazy (tickery + reguły)
make backup          # backup bazy do backups/
make restore         # restore z najnowszego backupu
make stats           # statystyki bazy
make shell-app       # shell do kontenera app
make shell-db        # shell psql
make help            # lista komend
```

Alternatywnie docker compose bezpośrednio:

```bash
# Laptop / desktop (bazowy compose — po usunięciu FinBERT nie ma GPU overridów)
docker compose up -d

# Jetson (aarch64) — app /proc/sys binds + pgadmin wyłączony
docker compose -f docker-compose.yml -f docker-compose.jetson.yml up -d

# Weryfikacja
curl http://localhost:3000/api/health                     # status systemu
curl http://localhost:3000/api/health/stats                # totale per tabela
curl http://localhost:3000/api/sentiment/insider-trades    # transakcje insiderów (Form 4)

# Testy integracji API (Faza 0)
npm run test:all
```

## Setup

1. `cp .env.example .env` i uzupełnij klucze API
2. `make up` — start stacku (autodetekcja środowiska)
3. `make seed` — seed tickerów i reguł alertów
4. Otwórz `http://localhost:3001` — dashboard React

## Architektura

### Stan obecny (Sprint 11 — focus na edge)

Działający system end-to-end w 6 kontenerach Docker. Po Sprint 11 system skupia się na insider pipeline + PDUFA + korelacjach zamiast sentymentu.

1. **Warstwa zbierania danych** — 2 aktywne kolektory + 2 pomocnicze:
   - **SEC EDGAR** co 30 min (CRON `5,35 * * * *` UTC) — Form 4 (insider trades) + 8-K (material events). Eventy `NEW_INSIDER_TRADE` / `NEW_FILING`.
   - **Options Flow** (Polygon.io EOD) CRON 22:15 UTC — volume spike detection (3× avg20d). Event `NEW_OPTIONS_FLOW`.
   - **PDUFA.bio** co 6h (CRON `15 */6 * * *` UTC: 00:15/06:15/12:15/18:15) — kalendarz dat FDA. Event `NEW_PDUFA_EVENT`. Używany do PDUFA boost w options scoring. `runCollectionCycle` output zawiera `{count, parsed, action?}` (TASK-06, 23.04.2026): `count` = nowe eventy, `parsed` = wiersze z parsera (zdrowie scrapera), `action='PARSER_EMPTY'` gdy parser zwrócił 0 (HTTP OK ale strona zmieniła strukturę) → warn level w system_logs.
   - **WYŁĄCZONE (Sprint 11)**: StockTwits (77% wolumenu, 0% edge), Finnhub news/MSPR (HFT lag), Reddit (placeholder). Schedulery StockTwits i Finnhub czyszczą repeatable jobs przy starcie (zero pustych jobów BullMQ). Finnhub `/quote` zachowany dla Price Outcome Tracker.
2. **Warstwa AI** — SEC Filing GPT Pipeline (bez sentymentu):
   - **Form4Pipeline** (`src/sec-filings/pipelines/form4.pipeline.ts`): GPT analiza insider trades. Filtr: **discretionary only** (is10b51Plan=true → skip), **pure Director SELL → SKIP_DIRECTOR_SELL** (Sprint 15 anty-sygnał), **non-C-suite + non-Director SELL → SKIP_NON_ROLE_SELL** (TASK-02 22.04.2026, V4 backtest H2 zero edge dla każdej SELL), **C-suite priorytet** (whitelist CEO/CFO/COO/CTO/CMO=Medical/CSO/CLO/CIO + President/Chairman/EVP+context → boost). **Multi-transaction Form 4 aggregation** (TASK-03 22.04.2026): collector grupuje fills w obrębie filing'u po (insiderName, transactionType, is10b51Plan) i emituje JEDEN event z aggregate totalValue/shares + `aggregateCount` + `aggregateTradeIds`. Prompt GPT dostaje adnotację "×N fills — split-fill execution". Rozwiązuje ASX case (22.04: 4 SELL Chen Tien-Szu $247M widziane jako 1 trade $152M po throttle). Decision tree udokumentowany inline w `onInsiderTrade()`. Prompt z 30-dniową historią, sign convention SELL=ujemna/BUY=dodatnia, safety net post-GPT. Conviction [-2,+2] → [-1,+1] do CorrelationService.
   - **Form8kPipeline** (`src/sec-filings/pipelines/form8k.pipeline.ts`): GPT per-Item prompty (1.01/2.02/5.02/other). Item 5.02 prompt rozróżnia **voluntary+successor vs crisis vs relief rally**. Item 1.03 Bankruptcy → natychmiastowy CRITICAL bez GPT. Zod walidacja, daily cap 20/ticker/dzień.
   - **Options Flow Scoring** (`src/options-flow/`): heurystyczny scoring (bez GPT). Spike ratio > 1000 → suspicious, conviction ×0.5. PDUFA boost ×1.3 gdy event < 30 dni. Standalone alert **tylko z pdufaBoosted=true**.
   - **CorrelationService** (`src/correlation/`): **3 aktywne wzorce** — INSIDER_CLUSTER (2+ Form 4 w 7d, **SELL only** — BUY disabled TASK-09 23.04.2026 po V5 cluster vs solo p>0.37, SELL observation mode Sprint 15), INSIDER_PLUS_8K (insider + 8-K w 24h), INSIDER_PLUS_OPTIONS (insider + opcje w 120h/5d). Redis Sorted Sets. 3 wyłączone wzorce (wymagały sentymentu). **Content-hash deduplikacja runPatternDetection** (TASK-04, 22.04.2026): in-memory Map `lastPatternHash` per ticker, hash = sorted `(patternType + signalIds)`, 15-min window → powtórzone wywołania bez zmiany składu patternów zwracają `PATTERNS_DETECTED_DUPLICATE` bez wołania `triggerCorrelatedAlert`. Rozwiązuje HPE cascade (7 detekcji w 6 min = noise). PATTERN_THROTTLE (2h OPTIONS / 24h CLUSTER) pozostaje jako alert-level ostateczna bariera. `triggerCorrelatedAlert` ma `@Logged('correlation')` — wcześniej DEDUP_SKIP / SKIP_LOW_CONVICTION były niewidoczne w system_logs.
   - **Anthropic Claude Sonnet** (`AnthropicClientService`, SDK `@anthropic-ai/sdk`) — bezpośrednie wywołanie API z NestJS. Zastąpił Azure OpenAI gpt-4o-mini (Sprint 12). Provider alias: `AzureOpenaiClientService` → `AnthropicClientService` (zero zmian w pipeline'ach). Konfiguracja: `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL` (domyślnie `claude-sonnet-4-6`). Azure VM (`74.248.113.3:3100`) na standby jako fallback.
   - **Pipeline observability (Tier 1)**: `system_logs` z 5 enriched kolumnami: `trace_id`, `parent_trace_id`, `level` (debug/info/warn/error), `ticker`, `decision_reason`. Decorator `@Logged()` z `extractLogMeta()` — automatyczna ekstrakcja metadata z args/result. Warn-level actions: `ALERT_TELEGRAM_FAILED`, `REDIS_ERROR`, `PARSER_EMPTY` (TASK-06). Tiered cleanup: debug 2d, info 7d, warn+error 30d. traceId propagacja: SEC EDGAR (per filing + per trade), Options Flow, PDUFA.
   - **Price Outcome Tracker** (`src/price-outcome/`): priceAtAlert (Finnhub /quote) w momencie alertu. CRON co 1h price1h/4h/1d/3d (NYSE open only), `@Cron('0 * * * *', { timeZone: 'UTC' })`, hard timeout 7d. **Sloty liczone od otwarcia NYSE** (`getEffectiveStartTime()`) — alerty pre-market (Options Flow 22:15, SEC 7:00) mają 1h/4h od open 9:30 ET, nie od czasu alertu (fix: price1h ≠ price4h).
   - **AlertEvaluator**: 8 aktywnych reguł, 12 wyłączonych. Per-symbol daily limit 5 alertów. Throttling per (rule, symbol, catalyst_type). `onInsiderTrade()` ma early return (Sprint 11 — Insider Trade Large wyłączone, obsługiwane przez Form4Pipeline). **Granularne action values** (Tier 1): `ALERT_SENT_TELEGRAM`, `ALERT_TELEGRAM_FAILED`, `ALERT_DB_ONLY_OBSERVATION`, `ALERT_DB_ONLY_DAILY_LIMIT`, `ALERT_DB_ONLY_CLUSTER_SELL_NO_EDGE`, `ALERT_DB_ONLY_CSUITE_SELL_NO_EDGE`, `ALERT_DB_ONLY_SELL_NO_EDGE`. `onSentimentScored` + 5 prywatnych checkerów + SILENT_RULES wycięte 22.04.2026 razem z FinBERT cleanup.
   - **AlertDispatcherService** (TASK-01, 22.04.2026, `src/alerts/alert-dispatcher.service.ts`): centralny punkt decyzji dispatch alertów. Jedna metoda `dispatch({ticker, ruleName, traceId, message, isObservationTicker?, isSellNoEdge?, isCsuiteSellObservation?, isClusterSellObservation?, isSilent?, bypassDailyLimit?})` z `@Logged('alerts')` → każdy dispatch widoczny w `system_logs`. Priority order suppression: observation > sell_no_edge > csuite_sell_no_edge > cluster_sell_no_edge > silent_rule > daily_limit. `bypassDailyLimit=true` dla bankruptcy (Item 1.03, FLAG #10 exempt) i standalone options flow alertów. Zastąpił ~200 LoC duplikowanej logiki suppression z 5 pipeline'ów (Form4, Form8k×2, Correlation, OptionsFlow, AlertEvaluator). Audyt logów 22.04.2026 ujawnił brak finalnego dispatch log entry — ten serwis zamyka gap.
   - **USUNIĘTE (22.04.2026)**: FinBERT sidecar + sentiment pipeline (listener, processor, HTTP client, entities sentiment_scores/ai_pipeline_logs jako orphan tabele, backfill seed, 6 reguł sentymentowych w DB nadal jako isActive=false audit trail). Backtest Sprint 15 potwierdził zero edge na sentymencie — usunięcie zamyka 1337 LoC martwego kodu.
3. **Warstwa danych** — PostgreSQL z 12 aktywnymi entities (14 tabelami: `sentiment_scores` i `ai_pipeline_logs` orphan po FinBERT cleanup, zachowane dla historycznych danych do drop-migration w Sprint 18), w tym `options_flow`, `options_volume_baseline`, alerts z 7 polami price outcome + `archived` + `nonDeliveryReason`, system_logs z 5 kolumnami Tier 1. Redis dla kolejek BullMQ + Sorted Sets (korelacje) z password support. TypeORM z `synchronize: true` (constant, niezależny od NODE_ENV).
4. **Warstwa dostarczania** — Dashboard React (MUI 5 + Recharts) na :3001 z MUI Tabs (Dashboard + Signal Timeline + System Logs + Słownik). Panel Status Systemu (kolektory, błędy, pipeline). **Visual distinction dla observation alertów** (TASK-05, 22.04.2026): `Alert.nonDeliveryReason` eksponowany w 3 endpointach (`/api/alerts`, `/outcomes`, `/timeline`); `PriorityChip` zmienia color z `error/warning` na `default` + `variant="outlined"` + label `"CRITICAL (obserwacja)"` gdy alert ma `nonDeliveryReason !== null`. Conviction pozostaje w DB bez cap (magnitude zachowana do retrospective backtest). Signal Timeline panel PRIORITY: przytłumiony kolor + suffix reason. Signal Timeline (`/api/alerts/timeline`) — sekwencja sygnałów per ticker z deltami cenowymi, gap czasowym, conviction, zgodność kierunków. **Options Flow tabela** (Sprint 16): 8 kolumn (z 11), żywy DTE z `expiry`, kolumny połączone (Ticker+kierunek+typ, Strike/Cena/OTM, Volume/Spike), nowe: Kontrakty (clustering per ticker+sesja), Ekspozycja (szt + notional), Conviction z paskiem siły. Sortowanie domyślne po conviction desc. Filtr wygasłych opcji (frontend + backend). Signal Timeline: dropdown pokazuje wszystkie tickery z alertami (usunięto filtr `priceAtAlert IS NOT NULL` i `HAVING COUNT >= 2`). **System Logs (Tier 1)**: 3 nowe kolumny (Level chip, Ticker mono, Decision Reason z kolorowymi chipami), 2 filtry (level dropdown, ticker input), trace_id w rozwinięciu wiersza z copy-to-clipboard. Alerty Telegram po polsku. REST API (31 endpointów, w tym 3 za `ApiTokenGuard`: trace, ticker, decisions) na :3000.

### Stack technologiczny

- **Backend**: NestJS 10, TypeORM, BullMQ, EventEmitter2, Node.js 20, TypeScript 5.x
- **Frontend**: React 18, Recharts 3.7, MUI 5 (dark theme), Vite 4
- **AI/NLP**: **Anthropic Claude Sonnet** (SDK `@anthropic-ai/sdk`, bezpośrednio z NestJS — zastąpił Azure OpenAI gpt-4o-mini w Sprint 12). FinBERT sidecar usunięty 22.04.2026 (Sprint 15 backtest: zero edge na sentymencie).
- **Infra**: Docker Compose (5 kontenerów: app, frontend, postgres, redis, pgadmin — pgadmin wyłączony na Jetsonie przez `profiles: [pgadmin]`), PostgreSQL 16 + TimescaleDB, Redis 7, serwer produkcyjny (autostart z git pull). Azure VM (74.248.113.3:3100) na standby jako fallback LLM.

### Usługi i porty

| Usługa | Port | URL |
|--------|------|-----|
| NestJS API | 3000 | http://localhost:3000/api/ |
| Frontend React | 3001 | http://localhost:3001/ |
| pgAdmin | 5050 | http://localhost:5050/ |
| PostgreSQL | 5432 | — |
| Redis | 6379 | — |

### Monitorowane sektory

- **Healthcare** (core): [doc/stockpulse-healthcare-universe.json](doc/stockpulse-healthcare-universe.json) — 37 tickerów (wg podsektora), 201 słów kluczowych, 18 subredditów, 20 reguł alertów (8 aktywnych, 12 wyłączonych). Backtest-backed edge (insider BUY d=0.43).
- **Semi Supply Chain** (observation mode): [doc/stockpulse-semi-supply-chain.json](doc/stockpulse-semi-supply-chain.json) — 14 tickerów w 3 koszykach: Memory Producers (MU, WDC, STX), Equipment & Packaging (KLIC, AMKR, ONTO, CAMT, NVMI, ASX), OEM Anti-Signal (DELL, HPQ, HPE, SMCI, NTAP). Alerty zapisywane do DB z `delivered=false`, `nonDeliveryReason='observation'` — brak Telegramu dopóki backtest nie potwierdzi edge'u. Plan: [doc/plan-semi-supply-chain.md](doc/plan-semi-supply-chain.md).

### Dokumentacja szczegółowa

- **Status projektu i plan**: [doc/PROGRESS-STATUS.md](doc/PROGRESS-STATUS.md)
- **Struktura plików**: [doc/schematy.md](doc/schematy.md)
- **Architektura (wizualizacja)**: [doc/stockpulse-architecture.jsx](doc/stockpulse-architecture.jsx)
- **Raporty tygodniowe**: [doc/reports/](doc/reports/) — analizy systemu z danymi z bazy
- **Changelog zmian**: [doc/reports/2026-03-14-zmiany.md](doc/reports/2026-03-14-zmiany.md) — ostatnie zmiany z uzasadnieniem

### Ważne konwencje danych

- **insider_trades.transactionType**: pełne słowa (`SELL`, `BUY`, `EXERCISE`, `TAX`, `GRANT`, `OTHER`), NIE kody SEC (`P`, `S`). Zawsze filtruj po pełnych słowach.
- **insider_trades.is10b51Plan**: `true` = automatyczny plan sprzedaży (szum, **skipowane w Form4Pipeline od Sprint 11**), `false` = discretionary (realny sygnał insiderski).
- **C-suite detection** w Form4Pipeline: regex na insiderRole — CEO, CFO, COO, CMO, CTO, President, Chairman, EVP, Chief, Executive Vice → boost priority.
- **Options flow spikeRatio > 1000**: flaga suspicious, conviction ×0.5 (anomalia danych Polygon, np. MRNA 5032×).
- **priceAtAlert**: od Sprint 11 zapisywany dla WSZYSTKICH typów alertów (Correlated Signal, Form 4, 8-K, Options). Wcześniej NULL dla 120+ alertów/2tyg.
- **tickers.sector**: `'healthcare'` (domyślny) lub `'semi_supply_chain'`. Używany do healthcare boost guard w Form4Pipeline (`sector === 'healthcare'` → ×1.2).
- **tickers.observationOnly**: `true` = alert zapisywany do DB ale NIE wysyłany na Telegram. Observation gate w Form4Pipeline, Form8kPipeline, AlertEvaluator.
- **alerts.nonDeliveryReason**: `'observation'` / `'silent_hour'` / `'daily_limit'` / `'csuite_sell_no_edge'` / `null`. Rozróżnia powód `delivered=false` — krytyczne dla forward analysis i backtestów. `'csuite_sell_no_edge'` dodane w Sprint 16b #2 (V4 backtest: H2 SINGLE_CSUITE SELL d=-0.002 p=0.95 → zero edge).

## Multi-środowisko: Laptop ↔ Jetson

Projekt działa na dwóch maszynach. Kod jest wspólny (git), konfiguracja osobna per maszynę.

### Środowiska

Po usunięciu FinBERT sidecara (22.04.2026) wszystkie maszyny używają identycznego
backendu Node.js — różnice ograniczają się do bindów Jetsona (`/proc`, `/sys`) i
profili pgAdmin.

| Środowisko | Architektura | Compose |
|------------|-------------|---------|
| **Laptop / desktop** | x86_64 | `docker-compose.yml` (bazowy) |
| **Jetson Orin NX** | aarch64 | `docker-compose.yml` + `docker-compose.jetson.yml` (app /proc /sys binds, pgadmin wyłączony) |

### Workflow

```
Laptop WSL2 (dev) ──git push──→ GitHub ──git pull──→ Serwer prod (192.168.0.138, autostart po reboot)
                                                  └→ Azure VM (74.248.113.3, PM2: processor.js + api.js)
```

- **Rozwijasz kod na laptopie**, commitujesz, pushujesz
- **Jetson po restarcie** automatycznie robi `git pull` + `make up` (crontab @reboot, skrypt `scripts/autostart.sh`)
- `.env` jest **gitignored** — każda maszyna ma swoje klucze API
- `make up` / `make rebuild` — Makefile sam wykrywa środowisko po `uname -m`

### Transfer bazy między maszynami

```bash
# Na źródłowej maszynie
make backup
scp backups/stockpulse_*.dump user@<cel-ip>:~/stockPulse/backups/

# Na docelowej maszynie
make restore
```

### Dokumentacja Jetson

Pełna dokumentacja setupu Jetsona: [doc/JETSON-SETUP.md](doc/JETSON-SETUP.md)

## Zmienne środowiskowe

Patrz `.env.example`. Plik `.env` jest **gitignored** — osobny na każdej maszynie.

Główne grupy:
- **Reddit**: OAuth2 (client ID, secret, username, password)
- **Finnhub**: klucz API (free tier, 60 req/min)
- **SEC EDGAR**: User-Agent z emailem (bez klucza, 10 req/sec)
- **Anthropic**: klucz API (`ANTHROPIC_API_KEY`) — **wymagany** dla SEC Filing Pipeline (Claude Sonnet). Opcjonalnie: `ANTHROPIC_MODEL` (domyślnie `claude-sonnet-4-6`), `ANTHROPIC_TIMEOUT_MS` (domyślnie 30000)
- **Polygon.io**: `POLYGON_API_KEY` — opcjonalny (Options Flow collector)
- **Azure Analysis Service**: URL do VM z gpt-4o-mini + timeout (legacy fallback — nieaktywne od Sprint 12, VM na standby)
- **Telegram**: token bota + chat ID do alertów
- **StockTwits**: publiczne endpointy, bez autoryzacji (200 req/hour)
- **Admin API**: `ADMIN_API_TOKEN` — opcjonalny, wymagany dla `/api/system-logs/trace`, `/api/system-logs/ticker`, `/api/system-logs/decisions`
- **Bazy danych**: konfiguracja PostgreSQL i Redis (z opcjonalnym `REDIS_PASSWORD`)

dev tak sie uruchamia 

wsl -d Ubuntu bash -c "cd /home/n1copl/stockPulse && docker compose up -d 2>&1 | tail -20"

## Sprint 16 P0 fixes completed (17.04.2026)

Commits c2d8ae9..7fe870b, szczegóły: [HANDOFF-CODE-REVIEW-2026-04-16.md](doc/HANDOFF-CODE-REVIEW-2026-04-16.md)
- FLAG #30: Form 4 multi-reportingOwner parser (mergeOwnerRoles + pure Director SELL)
- FLAG #25: disable broken PriceOutcome backfill (getQuote current price ≠ historical)
- FLAG #21: winsorize options baseline (spike contamination, camouflage effect)
- FLAG #8: bankruptcy detection before daily cap (Item 1.03 nie wymaga GPT)
- FLAG #26: NYSE holidays 2024-2027 (isNyseHoliday w isNyseOpen)
- FLAG #10: AlertDeliveryGate shared daily limit (4 pipelines, bankruptcy exempt)

## Sprint 16b interim fixes (17.04.2026, po decyzji 17.04)

Action items po analizie 24h logów produkcji — briefing "Post Sprint 16 action items".
- #3 AlertEvaluator.onInsiderTrade: dead handler usunięty (Sprint 11 przeniósł logic do Form4Pipeline, handler generował SKIP_RULE_INACTIVE spam 12×/dobę)
- #4 OptionsFlow: AbortSignal.timeout 30s na Polygon fetchach (17.04 produkcja: runCollectionCycle duration=11h 25min bez timeout — analogiczne do FLAG #28)
- #7 8-K pipeline: diagnoza bez zmian kodu — SKIP_NOT_8K w logach to Form 4/3 filingi (poprawne), 8-K pipeline działa (2 real 8-K/7 dni = post-earnings low activity, nie bug)
- #1 Form4Pipeline C-suite whitelist: `/\bChief\b/i` zastąpione explicit whitelist (soft roles Comm/People/Diversity/Marketing/Sustainability wyłączone). 17.04 "Chief Communications Officer" dostawał ×1.3 boost i Telegram alert — noise. Chief Medical Officer ZOSTAJE (healthcare critical), Chief Marketing Officer WYŁĄCZONY (decyzja Przemka).
- #2 Form4Pipeline C-suite SELL → observation mode: V4 backtest potwierdził zero edge (H2 SINGLE_CSUITE all_sells N=855 d=-0.002 p=0.95). Route do DB-only z `nonDeliveryReason='csuite_sell_no_edge'`, action `ALERT_DB_ONLY_CSUITE_SELL`. GPT analysis zachowana dla forward validation (DB record ma conviction + priceAtAlert). C-suite BUY dalej idzie na Telegram (d=0.83 vs baseline, 1.3× boost).

## Sprint 17 P1 research (18.04.2026, mierzalne)

- #1 Form4Pipeline Director BUY boost ×1.15: V4 potwierdził d=0.59 dla Director BUY
  (mniejsze niż C-suite d=0.83 ×1.3, ale wyraźny sygnał). Kumulatywne z healthcare
  ×1.2 (Dir hc BUY = ×1.38). C-suite priorytet w co-filing (albo/albo, nie stack).
- #2 Backtest control group fix: usunięty top-level `is_healthcare==True` filter
  z `run_analysis`. H1-H5 filtrują healthcare per-hypothesis (tx_df_hc), H6 używa
  pełnego tx_df (healthcare + control). Bez tego H6 miał 0 control events — teraz
  faktyczny sector-specific edge test możliwy.
- #3 H1 cluster vs single BUY: nowa sub-analiza w analyze_h1_clusters —
  direct Welch's t-test cluster BUY vs non-cluster single BUY (unique_insiders<2
  w 7d forward window). Odpowiada "czy warto czekać na drugiego insidera".
  Funkcje: `_collect_single_buy_events`, `_direct_cluster_vs_single`. 7 testów
  jednostkowych w `tests/test_analyzer.py`.

## Scheduler consolidation (22.04.2026)

Konsolidacja wszystkich zaplanowanych zadań — eliminacja niedeterministycznego dryfu po restarcie + rozkład slotów żeby uniknąć jednoczesnego startu 3 kolektorów.

**Audyt**: 2 `@Cron` (nestjs/schedule) + 3 aktywne `BullMQ repeatable` (sec-edgar, pdufa-bio, options-flow) + 3 wyłączone schedulery (finnhub, stocktwits — cleanup-only po Sprint 11; reddit — placeholder). Zmiany:

- **timeZone UTC w `@Cron` (48caf0b)**: `price-outcome.service.ts:49` i `system-log.service.ts:144` używały TZ serwera (laptop CEST / Jetson UTC / prod różne). Explicit `{ timeZone: 'UTC' }` zapewnia spójność.
- **every:ms → cron pattern (ee14a5f)**: BullMQ `every: N ms` dryfował od momentu startu (restart o 08:17 → odpala :47, :17...). Fix: `sec-edgar` pattern `0,30 * * * *` UTC, `pdufa-bio` pattern `0 */6 * * *` UTC. Deterministyczny timing.
- **5-min stagger (de11193)**: po migracji sec-edgar/pdufa-bio lądowały na :00 — kolizja z `price-outcome` (hourly). Rozkład:
  - `:00` — price-outcome (Finnhub /quote)
  - `:05`, `:35` — sec-edgar (co 30 min, pattern `5,35 * * * *`)
  - `:15` — pdufa-bio (co 6h, pattern `15 */6 * * *`)
  - `03:00` — system-log cleanup (daily)
  - `16:30 America/New_York` — options-flow (DST-aware, bez kolizji z UTC slotami)

SEC rate limit zweryfikowany: `sec-edgar.service.ts:68` 200ms delay per ticker (sekwencyjnie, ~4-5 req/s vs limit 10), User-Agent z emailem wymuszany przez Joi (`env.validation.ts:30`).

## Produkcyjny 10b5-1 parser (status OK, zweryfikowano 17.04)

`form4-parser.ts:148-152` używa **per-transaction** XML path `txn.transactionCoding?.['Rule10b5-1Transaction']` + strict value match ('1' albo 'Y'). NIE jest naive string detection (odróżnia się od V3 Python FLAG #34). 4 testy jednostkowe pokrywają edge cases ('1', 'Y', pusty string, brak tagu).

## Known issues NOT yet fixed (priorytet Sprint 18)

- FLAG #41-43: Python backtest — baseline sampling uniform per-ticker (#42 skipped w HANDOFF #2), inne nierozstrzygnięte
- Sprint 18 candidates post-V5: ~~INSIDER_CLUSTER BUY disable~~ ✅ TASK-09 23.04.2026, ~~C-suite regex ujednolicenie~~ ✅ TASK-10 23.04.2026, ~~d=None bug w `_direct_cluster_vs_single`~~ ✅ TASK-11 23.04.2026 (stale doc entry), ~~report_generator render `hc_vs_ctrl_direct` + `cluster_buy_vs_single_buy`~~ ✅ TASK-12 23.04.2026

## Already resolved (Sprint 16b + Sprint 17 P1)

- FLAG #30 multi-owner parser: fixed (TypeScript mergeOwnerRoles, Python eabdb06)
- FLAG #32 Cohen's d biased: fixed (ac503d7 proper pooled formula + winsorization)
- FLAG #34 naive 10b5-1 detection (Python): fixed (eabdb06 per-transaction); produkcyjny TS parser OK
- FLAG #35 10b5-1 per-transaction: fixed (eabdb06)
- FLAG #37 brak Bonferroni: fixed (d7a86d6)
- FLAG #40 H6 niewymienne baselines: fixed (d7a86d6 common baseline + e07bbc2 control fix)
- FLAG #28 SEC EDGAR fetch bez timeout: fixed (7e63d9d AbortSignal.timeout 30s na fetchUrl + fetchText, analogicznie do Options Flow d78a92f)
- Scheduler consolidation: fixed (48caf0b timeZone UTC w @Cron, ee14a5f every:ms → cron pattern dla sec-edgar+pdufa-bio, de11193 5-min stagger :00/:05/:15). Eliminuje dryf po restarcie i kolizje o :00 UTC.
- FinBERT + sentiment pipeline removal (22.04.2026): fixed (988bf03 docker-compose + Makefile, ba45976 sentiment services + test, d3a1b5c cross-module + env, b3a2f2b entities + API, 4b117db finbert-sidecar dir). Backtest Sprint 15 potwierdził zero edge na sentymencie (2 tygodnie × 962 alerty × 55.5% hit rate). Usunięto: kontener FinBERT, 3 compose services/volumes, cpu.yml override, 3 Makefile targets, FINBERT_SIDECAR_URL env, FinbertClient + SentimentListener + SentimentProcessor + test, 5 checkerów sentymentowych w AlertEvaluator, SILENT_RULES, EventType.SENTIMENT_SCORED, QUEUE_NAMES.SENTIMENT, SignalDirection, 6 metod telegram-formatter, 2 entities + exports, 3 endpointy REST, 4 sekcje weekly-report SQL, pipelineStats w system-overview, backfill-sentiment seed, finbert-sidecar/ (7 plików Python/Docker). Łącznie ~2400 LoC usunięte z repo. Tabele `sentiment_scores` + `ai_pipeline_logs` zachowane jako orphan (historyczne dane, drop-migration w Sprint 18).
- TASK-01 AlertDispatcherService observability gap (22.04.2026): fixed (commit 6191a81). Audyt logów produkcji 500 wpisów, 18h okno: system_logs pokazywały STORED / PATTERNS_DETECTED / THROTTLED, ale NIE widać było "czy alert poszedł na Telegram i dlaczego nie". Nowy serwis `src/alerts/alert-dispatcher.service.ts` centralizuje finalną decyzję dispatch (1 metoda `dispatch()` z `@Logged('alerts')`). Refactor 5 konsumentów (Form4Pipeline, Form8kPipeline×2, CorrelationService, AlertEvaluator.sendAlert, OptionsFlowAlertService) usunął ~200 LoC duplikowanej logiki suppression. Priority order: observation > sell_no_edge > csuite_sell > cluster_sell > silent > daily_limit. Test jednostkowy `test/unit/alert-dispatcher.spec.ts` — 10 cases (priority order + bypassDailyLimit + telegram_failed).
- TASK-02 SKIP_NON_ROLE_SELL hard skip (22.04.2026): fixed. Audyt logów 22.04: GM "ASE Inc. Chung-Li Branch" (Chen Tien-Szu) ASX SELL $152M przeszedł przez Form4Pipeline → conviction -0.5 → CorrelationService → fałszywy CRITICAL "INSIDER + Unusual Options" w portalu (semi ticker observation route nie blokował korelacji — TASK-05 niezależny). Decision tree krok 4b dodany w `Form4Pipeline.onInsiderTrade` PRZED daily cap i observation gate: `transactionType==SELL AND !isCsuiteRole AND !isDirectorRole → SKIP_NON_ROLE_SELL`. Logika: V4 H2 SINGLE_CSUITE SELL N=855 d=-0.002 zero edge → non-role SELL tym bardziej. Nowa eksportowana `isDirectorRole()` (parallel do `isCsuiteRole`). Decision tree pełny komentarz inline (8 kroków). 29 nowych testów w `test/unit/form4-pipeline-rules.spec.ts` (14 isDirectorRole + 15 SKIP_NON_ROLE_SELL z ASX case). 10% Owner SELL też trafia tu — żadna hipoteza H1-H6 nie testowała oddzielnie, zostaje skip do retroactive H7.
- TASK-05 observation gate visual distinction w portalu (22.04.2026): fixed. Investigation pokazał, że backend DB + Telegram gate działają poprawnie (ASX correlated alert saved with `delivered=false, nonDeliveryReason='observation'`), gap był **tylko frontend**: 3 endpointy API zwracały alerty bez pola `nonDeliveryReason`, a `PriorityChip` renderował wszystkie jako CRITICAL/error color niezależnie od `delivered`. Fix: (1) backend `alerts.controller.ts` — dodane `delivered + nonDeliveryReason` w response `.map()` dla `/outcomes` i w SQL SELECT dla `/timeline` + `/recent-timeline` (endpoint `/alerts` już zwraca pełny entity). (2) frontend `api.ts` — dodane pole `nonDeliveryReason` do `Alert` interface. (3) `PriorityChip` w `App.tsx` — gdy `row.nonDeliveryReason != null`: color `default` (szary), variant `outlined`, label `"${priority} (${nonDeliveryLabel})"`. Mapowanie `nonDeliveryLabel`: `observation → obserwacja, csuite_sell_no_edge → C-suite SELL (zero edge), sell_no_edge → SELL (zero edge), silent_hour → cicha godzina, daily_limit, telegram_failed`. (4) `SignalTimeline.tsx` — PRIORITY cell ma przytłumiony kolor + suffix reason. **Odrzucono Opcja B (conviction cap -0.30):** maskowałaby magnitude w DB, co utrudniłoby retrospective backtest H2 (Python re-run zaplanowany w TASK-11). Zachowana pełna conviction (-0.70 dla ASX case) — UI tylko visual distinction, dane pozostają bez zmian.
- TASK-04 CorrelationService pattern detection deduplication (22.04.2026): fixed. HPE cascade 22.04 20:33-20:38: 7 PATTERNS_DETECTED w 6 min (patterns=1/1/2/2/1/2/2), każdy re-run `runPatternDetection` po nowym signalu w Redis Sorted Set. Tylko 1 Telegram alert wyszedł (PATTERN_THROTTLE 2h łapał DEDUP_SKIP) — cascade to marnowanie CPU + log clutter, nie alert spam. Dodano: (1) content-hash dedup w `runPatternDetection` — in-memory `Map<ticker, {hash, ts}>` z 15-min window, hash = sha256(sorted (type + signalIds)) truncated 16 chars; powtórzone detekcje z tym samym składem zwracają `PATTERNS_DETECTED_DUPLICATE` bez wołania `triggerCorrelatedAlert`. (2) `@Logged('correlation')` na `triggerCorrelatedAlert` — wcześniej DEDUP_SKIP / SKIP_LOW_CONVICTION / dispatch actions były niewidoczne w system_logs (gap w audycie TASK-01). Zmiana return shape z `Promise<string>` na `Promise<{action, ticker, patternType}>` żeby extractLogMeta wyciągnął decisionReason. Exported pure functions `hashPatternSet()` + `shouldSkipDuplicatePatternDetection()`. 20 nowych testów w `test/unit/correlation.spec.ts` (hash stability, order-insensitivity, window boundary, HPE cascade scenario). Odrzucone z propozycji TASK-04: TTL cleanup signals (już działa via ZREMRANGEBYSCORE on storeSignal), 30-min per-pattern rate limit (PATTERN_THROTTLE 2h wystarcza), Redis cache hash (in-memory prostsze, post-restart koszt = 1 niepotrzebny emit per ticker).
- TASK-03 multi-transaction Form 4 aggregation (22.04.2026): fixed. Audyt logów 22.04 ASX case: Chen Tien-Szu 4 SELL w jednym Form 4 (`accessionNumber_0.._3`) = 530k shares / $247M widziane jako 1 trade $152M bo parser emitował 4 NEW_INSIDER_TRADE eventy, throttle (alerts.sentAt) kasował 2-4 przed dotarciem do `correlation.storeSignal()`. Fix collector-level w `sec-edgar.service.ts parseAndSaveForm4`: save all rows (historia), potem groupBy (insiderName, transactionType, is10b51Plan) w obrębie filing'u + emit JEDEN event per grupa z aggregate totalValue/shares + `aggregateCount` + `aggregateTradeIds`. Form4Pipeline używa aggregate values w GPT prompt (`buildForm4Prompt` dostaje `aggregateCount`, dodaje note "Split-fill execution — market impact management lub broker TWAP"), Telegram message, correlation signal. Decision tree opcja: throttle już nie jest problemem bo 1 event na filing. Oszczędza N× GPT daily cap. 12 nowych testów w `test/unit/form4-aggregation.spec.ts` (grouping logic: ASX case, BUY+SELL split, plan+discretionary split, backward compat single-trade; prompt aggregate note rendering).
- TASK-06 PDUFA collector parser observability (23.04.2026): fixed. Diagnoza 7-dniowych logów z `count=0` we wszystkich 29 cyklach pokazała że scraper DZIAŁA — parser zwraca 14 events (aktualny pdufa.bio 2026), wszystkie zdedup'owane (dziś scraped_at update'owany). Dodatkowo 5 stale rows z marca 2026 (PDUFA dates z przeszłości) zostały zdjęte z pdufa.bio ale pozostają w DB bez update. Prawdziwy sygnał awarii to `parsed.length === 0` (HTTP OK ale 0 rows z parsera). Fix: `PdufaBioService.scrapeAndInsert()` zwraca `{inserted, parsed}` (wydzielone z `collect()` który nadal zwraca number dla kontraktu BaseCollectorService). Override `runCollectionCycle()` w `PdufaBioService` z `@Logged('collectors')` — output `{collector, count, parsed, action?}`. Gdy `parsed===0`, action='PARSER_EMPTY'. Dodany mapping `PARSER_EMPTY → level='warn'` w `logged.decorator.ts extractLogMeta()`. System Logs tab rozróżnia `{count:0, parsed:14}` (normalny dedup) vs `{count:0, parsed:0, action:'PARSER_EMPTY'}` (alarm z warn chip). 8 testów w `test/unit/pdufa-collector.spec.ts` (parser empty detection, warn action contract, integration runCollectionCycle z mocked fetchPage — dedup/empty/new/HTTP throw). **Odrzucone z task description**: "3+ consecutive zeros" alarm (false positive 90% roku — PDUFA calendar ma legit 2+ tygodniowe pauzy), Redis state tracking, admin `/api/health/collectors` endpoint (BaseCollectorService.getHealthStatus już istnieje).
- TASK-09 INSIDER_CLUSTER BUY disabled (23.04.2026): fixed. V5 backtest (commit f69cfa8) cluster_buy_vs_single_buy N_cluster=21 vs N_single=49, p=0.445 (1d) / 0.700 (3d) / 0.955 (7d) / 0.371 (30d) — czekanie na 2-giego insidera nie dodaje edge'u nad solo BUY. Solo BUY rule (Form 4 Insider BUY) pokrywa signal: V5 C-suite BUY d=+0.92 (7d), All BUY d=+0.75 (7d). Fix: `detectInsiderCluster` w `correlation.service.ts` zwraca `null` gdy `getDominantDirection==='positive'` (BUY) — SELL zostaje (observation mode w `triggerCorrelatedAlert` od Sprint 15). Usunięty stale V4 comment "BUY clusters d=0.47 p=0.009 nadal alertują normalnie" — V5 obalił. Historic audit 90d: 0 INSIDER_CLUSTER BUY alertów w DB (wszystkie 8 istniejące to SELL observation), więc retroactive archive nie potrzebny. Defence-in-depth: flaga `isClusterSellObservation` w `triggerCorrelatedAlert` zostaje (BUY już nie trafi tu dzięki detection guard, ale krytyczna dla SELL). 6 nowych testów w `test/unit/correlation.spec.ts` TASK-09 block (2 BUY/3 BUY → null, 2 SELL → emit, 1/1 mixed → null, >7d window → null, non-form4 → null).
- TASK-07 AlertDeliveryGate stats audit (23.04.2026): fixed (scenariusz C, UX only). Audyt logów 22.04 Telegram raporty 8h pokazywały "Łącznie: 2 (dostarczono: 0)" przez całą dobę — wyglądało jak alarm. Diagnoza DB 48h (12 alertów): 11 observation (semi HPE/WDC/ASX/DELL/ONTO + GILD/HIMS semi-flag) + 2 sell_no_edge (HIMS Form 4 Sprint 17 SELL route) + 2 delivered. **0 daily_limit hits, 0 telegram_failed, 0 csuite_sell_no_edge.** Limit `MAX_TELEGRAM_ALERTS_PER_SYMBOL_PER_DAY=5` hardcoded (nie env), per-ticker, nie konsumowany przez raporty 8h (`SummarySchedulerService.sendSummary` wysyła bezpośrednio przez `telegram.sendMarkdown`, pomija `alertRepo.save`). Scenariusz A (gate bug) i B (limit za niski) odrzucone — gate działa poprawnie. Fix UX: `SummarySchedulerService.sendSummary` dodaje drugi query `GROUP BY nonDeliveryReason WHERE delivered=false AND nonDeliveryReason IS NOT NULL`; `formatSummary` renderuje sekcję _Niedostarczone (N):_ z PL etykietami ze static `REASON_LABELS` map (observation→Obserwacja, sell_no_edge→SELL (zero edge), csuite_sell_no_edge→C-suite SELL (zero edge), cluster_sell_no_edge→Cluster SELL (zero edge), silent_rule, silent_hour, daily_limit→Dzienny limit, telegram_failed). Po zmianie typowy raport pokazuje "Łącznie: 2 (dostarczono: 0) / Niedostarczone (2): Obserwacja: 2" — Przemek od razu widzi "to są observation, nie bug". **Testy**: `test/unit/alert-delivery-gate.spec.ts` 6 cases (under/at/over limit, UTC todayStart filter, static constant — pierwszy dedykowany test tego serwisu) + `test/unit/summary-scheduler.spec.ts` 5 cases (scenario C replay, no-suppressed, zero-alerts, unknown reason fallback, all 7 known reason labels). **Odrzucone z task description**: raport 8h jako self-consumer limit (`sendSummary` nie zapisuje do alerts entity), limit bump (5/ticker/dzień realistic dla 42-ticker universe), scenariusz A/B fix (brak bug'u).
- TASK-10 C-suite regex unification (23.04.2026): fixed. `form4.pipeline.ts:170` miało inline regex `/\bCEO|CFO|COO|CTO|President|Chair|Chief\b/i.test(role)` używany tylko w `isPureDirector` check (step 3 decision tree), podczas gdy step 4 SELL gate + step 7 BUY boost używały strict `isCsuiteRole()` whitelist (Sprint 16b). 2 różne definicje = niespójne semantyki: (1) broad regex łapał "Vice President" bez negative lookbehind, (2) `/\bChief\b/` łapał wszystkie Chief X vs strict whitelist tylko Executive/Financial/Operating/Technology/Information/Medical/Scientific/Legal/Accounting. Fix: linia 170 → `isCsuiteRole(role)` (bez `name` — step 3 sprawdza tylko role string, zgodnie z oryginalną semantyką). `C_SUITE_PATTERNS` rozszerzona o `/\bChair(man|woman|person)\b/i` + `/\bVice\s+Chair(man|woman|person)\b/i` — stary broad regex `\bChair` (prefix) też łapał te warianty, bez rozszerzenia cicha regresja dla Chairwoman/Chairperson. **Skutki semantyczne** (change list): Director + Vice President → teraz pure Director SELL → SKIP (stary bug: `\bPresident` bez lookbehind łapał VP). Director + Chief Marketing/Communications/People/Sustainability Officer → teraz pure Director SELL → SKIP (Sprint 16b decyzja że soft roles nie mają insider info — rozszerzenie do step 3). Director + Executive Vice President (bez finance/ops context) → teraz pure Director SELL → SKIP. Director + CEO/CFO/Chairman/CMO/President → bez zmiany (dalej hasCsuite=true, NIE skip, zgodnie z FLAG #30). 26 nowych testów w `test/unit/form4-pipeline-rules.spec.ts` (6 Chair variants + 20 isPureDirector co-filing coverage). 322/322 unit pass, `tsc --noEmit` clean. Docstrings `isDirectorRole` + inline step 3 comment zaktualizowane — kroki 3/4/7 decision tree identyczna definicja C-suite. **Odrzucone**: shared utility `src/common/utils/insider-roles.ts` (Form8kPipeline + AlertEvaluator nie mają C-suite logic — premature abstraction); "Chair" alone w whitelist (SEC filings używają Chairman/Chairperson z suffix).
- TASK-11 d=None bug w `_direct_cluster_vs_single` (23.04.2026): verified stale, not real bug. Cheatsheet linia 327 + CLAUDE.md "Sprint 18 candidates" twierdziły że `scripts/backtest/analyzer.py::_direct_cluster_vs_single` zapisuje `None` zamiast cohens_d w V5 JSON. Faktyczny inspect JSON `jq '.[0].sub_groups.cluster_buy_vs_single_buy.horizons'` pokazał realne wartości: 1d=+0.217 / 3d=-0.099 / 7d=-0.013 / 30d=-0.229. Commit `f69cfa8` message sam cytował "d [-0.23,+0.22]" — doc entry napisany przed regenerate nie zaktualizowany. Kod w `analyzer.py:473-487` poprawny: proper pooled_sd (FLAG #32 fix ac503d7) + `float(cohens_d)` serialization + guard `len(vals) < 3 → note="insufficient N"` dla małych N (N=21/49 prod V5 nie trafia tu). **Działania**: (1) regression test `TestDirectClusterVsSingle::test_v5_production_scenario_d_not_none` w `scripts/backtest/tests/test_analyzer.py` — realistyczny V5 scenariusz (N_cluster=21, N_single=49), asserts cohens_d is float dla wszystkich 4 horizons (1d/3d/7d/30d), blokuje regresję gdyby ktoś refaktoryzował pooled_sd/guard. (2) Cheatsheet linia 327 — wpisane faktyczne d values +0.22/-0.10/-0.01/-0.23 + notka TASK-11. (3) Cheatsheet "Known issues" #8 → oznaczone ✅ DONE. (4) CLAUDE.md Sprint 18 candidates (dwie linie) → ~~d=None bug~~ done. **Odrzucone**: print debugging + trace None source (task description), edit `_direct_cluster_vs_single` (kod już poprawny), re-run V5 backtest (JSON od f69cfa8 ma poprawne wartości). Testy: 15/15 pass (14 istniejących + 1 TASK-11 regression).
- FOLLOW-6 ASX regression integration test (23.04.2026): fixed. Sprint 18 trigger case (22.04.2026: GM "ASE Inc. Chung-Li Branch" Chen Tien-Szu SELL $152M na ASX → fałszywy CRITICAL "INSIDER + Unusual Options" w portalu z conviction -0.70) miał izolowane testy reguł (TASK-02 SKIP_NON_ROLE_SELL block + TASK-05 observation rendering frontend) ale brakowało **end-to-end acceptance test** dowodzącego że krok 4b w `Form4Pipeline.onInsiderTrade` faktycznie blokuje wszystkie side-effects. Pure-function tests reguł nie wyłapią regresji jeśli ktoś przesunie observation gate przed SKIP_NON_ROLE_SELL — regresja invisible aż do kolejnego ASX-case'a w produkcji. **Fix**: dodany `Form4Pipeline integration — ASX regression (FOLLOW-6)` describe block na końcu `test/unit/form4-pipeline-rules.spec.ts` z 1 testem instancjonującym pełny `Form4Pipeline` przez `new Form4Pipeline(...)` z 14 mockami (5 repo, 5 services, 4 optional). Test odpala `pipeline.onInsiderTrade({symbol: 'ASX', insiderRole: 'GM, ASE Inc. Chung-Li Branch', transactionType: 'SELL', totalValue: 152_554_997, ...})` i asseruje: (1) `result.action === 'SKIP_NON_ROLE_SELL'`, (2) **17 mocków `not.toHaveBeenCalled()`** — daily cap, GPT, telegram, formatter, dispatcher, correlation (storeSignal + schedulePatternCheck), deliveryGate, alertRepo (save + findOne), tradeRepo (findOne + save), filingRepo (findOne + save), tickerRepo, ruleRepo, tickerProfile, finnhub. **Eksport**: `Form4Pipeline` class musiał być dodany do imports z `form4.pipeline.ts` (był już exported, tylko nie używany w specs). `@Logged` decorator gracefully handle'uje brak `SystemLogService.getInstance()` (zwraca null → `logger?.log` skip), więc nie wymaga NestJS bootstrap w tescie. **Weryfikacja**: 326/326 unit pass (+1 FOLLOW-6, 13 suites), test exec 10ms — speed sam w sobie potwierdza zero side-effects (real DB/HTTP byłyby wolniejsze). `tsc --noEmit` clean. Brak rebuildu produkcji wymaganego (test-only zmiana).
- FOLLOW-2 usuń `as any` w SEC pipelines gptAnalysis persistence (23.04.2026): fixed. Audyt zgłosił 1 miejsce (form4.pipeline.ts:329), grep pokazał drugie (form8k.pipeline.ts:181) — ten sam root cause. Kolumna `gptAnalysis` w `src/entities/sec-filing.entity.ts:50` była typowana jako `Record<string, any>`, a pipelines przypisywały strukturalnie węższy `SecFilingAnalysis` (nested `price_impact` z enum direction, conviction, summary, key_facts). TypeScript traktuje `SecFilingAnalysis` jako niekompatybilny z `Record<string, any>` bo `price_impact` nie jest indexowany jako `any` — stąd `as any` jako workaround. **Fix**: import `SecFilingAnalysis` z `../sec-filings/types/sec-filing-analysis` (neutralny plik, zero circular imports — pipeliny i entity oba od niego zależą, nie odwrotnie) + zawężenie typu kolumny na `SecFilingAnalysis | null`. Oba `as any` w Form4Pipeline:329 i Form8kPipeline:181 usunięte — przypisania teraz type-safe. Brak nowych plików, zero zmian w strukturze. `tsc --noEmit` clean, 325/325 unit pass (13 suites). **Znalezione po drodze** (out of scope): `src/api/alerts/alerts.controller.ts:367-368` ma `null as any` przy resetowaniu `filing.gptAnalysis = null`/`filing.priceImpactDirection = null`. Po zawężeniu typu kolumny `as any` tam przestał być semantycznie potrzebny (`null` pasuje do `SecFilingAnalysis | null`), ale tsc nadal przechodzi bo `null as any` jest szerszy assignable cast. Kandydat na osobny cleanup jeśli audyt go łapie.
- FOLLOW-3 frontend Alert type consistency (nonDeliveryReason) (23.04.2026): fixed. Audyt wskazał `(a as any).nonDeliveryReason` w `SignalTimeline.tsx` i `row?.nonDeliveryReason as string | null | undefined` w `App.tsx`. Root cause: `frontend/src/api.ts` ma 2 interfejsy — full `Alert` (24 pola, endpoint `/api/alerts`, `/outcomes`) i uproszczony `TimelineAlert` (endpoint `/api/alerts/timeline`, `/recent-timeline`). `TimelineAlert` nie dostał `delivered + nonDeliveryReason` pól w TASK-05 (22.04.2026, visual distinction gate) mimo że backend `alerts.controller.ts:207-208` już je zwracał w SELECT — gap w propagacji typów frontend. `App.tsx` osobno: `PriorityChip` renderowany przez generyczny `DataPanel` z `row: any`, więc używał cast. **Fix** (Opcja A minimal — rozszerzenie lokalnych typów, NIE import pełnego `Alert`): (1) `frontend/src/api.ts:277-279` — do `TimelineAlert` dodane `delivered: boolean; nonDeliveryReason: string | null;`. (2) `SignalTimeline.tsx:415,422-428` — 6× `(a as any).nonDeliveryReason` → `a.nonDeliveryReason` (type-safe dostęp przez rozszerzone `TimelineAlert`). (3) `App.tsx:124-128` — `PriorityChip` props z `row?: any` → `row?: { nonDeliveryReason?: string | null }` (minimal structural type dla `DataPanel` row any-typed), usunięty cast `as string | null | undefined`. **Odrzucone**: Opcja B (import full `Alert` zamiast lokalnego `TimelineAlert`) — wymagałoby pełnego shape 24 pól przy użyciu w `DataPanel` any-row, nadmiar. **Weryfikacja**: `tsc --noEmit` clean dla zmienionych plików. Pre-existing errors w `SystemHealthPanel.tsx` (FinBERT cleanup 22.04 usunął `pipeline` z `SystemOverview` — nie w scope FOLLOW-3). `npm run build` ma pre-existing issue: `package.json` pinuje `typescript: ~4.9.5` + `tsconfig.json` ma `moduleResolution: bundler` (wymaga TS 5.0+) → konflikt istniał przed FOLLOW-3, nie wprowadzony tym fixem. **Znalezione po drodze**: `frontend/node_modules/` nie było installed na Jetsonie — `npm install` odpalony przez agenta (162 pakiety, 24s). `frontend/package-lock.json` wygenerowany (nie tracked w repo dotąd — zostawiony untracked, decyzja tracking to osobny task).
- FOLLOW-4 dispatchResult shape mismatch (23.04.2026): resolved w FOLLOW-1. Audyt wskazał że `suppressedBy` może być `null` (happy path alert delivered) albo `'dispatcher_unavailable'` (fallback) — logicznie OK, ale REASON_LABELS w raporcie 8h nie mapował `dispatcher_unavailable`. FOLLOW-1 commit `4cb8aa5` zamknął lukę przez `'dispatcher_unavailable': 'Dispatcher niedostępny'` w mapie. Nie wymaga osobnego commitu.
- FOLLOW-1 dispatcher_unavailable helper + label (23.04.2026): fixed. Audit ujawnił 3 problemy: (1) literał `'dispatcher_unavailable'` + `'ALERT_DB_ONLY_DISPATCHER_UNAVAILABLE'` duplikowany w 5 miejscach ternary-fallback (Form4:412, Form8k:222 main path, Form8k:318 bankruptcy, Correlation:596, OptionsFlow:189) — każde miało niezależny object literal, ryzyko divergence przy dodawaniu pól do `DispatchResult`. (2) `REASON_LABELS` w `summary-scheduler.service.ts:133` nie miał mapowania dla `dispatcher_unavailable` → raport 8h renderowałby raw string `"dispatcher_unavailable: N"` zamiast PL label. (3) Pozorna traceId asymetria (Form4/Form8k:222 mają traceId, 3/5 nie) zweryfikowana jako **false positive** — happy-path `dispatcher.dispatch(...)` też nie dostaje traceId w 3 miejscach (Form8k bankruptcy handler dostaje tylko `symbol+filing+ticker`, Correlation `triggerCorrelatedAlert` bez traceId, OptionsFlow standalone alert bez traceId). Fallback jest consistent z happy-path; nie ma co wymuszać spójności bo 3/5 pipeline'ów fundamentalnie nie mają traceId w scope. **Decyzja produktowa Przemek**: B (label) + Helper (A' lite), bez A (remove @Optional — ujemny ROI: Q1 walidacja wszystkich AlertsModule imports + Q2 update test mocków = 1-2h koszt, benefit = zamknięcie impossible state który w produkcji się nie zmaterializuje). **Fix**: (1) exported `buildDispatcherUnavailableFallback({ticker, ruleName, traceId?})` w `alert-dispatcher.service.ts:47-75` zwraca kompletny `DispatchResult` shape — jedno źródło prawdy, traceId opcjonalne (zgodne z rzeczywistym brakiem go w 3/5 pipeline'ów). (2) Wszystkie 5 ternary-fallback literałów zastąpione call do helpera (import `buildDispatcherUnavailableFallback` dodany do każdego konsumera obok `AlertDispatcherService`). (3) `REASON_LABELS` + docstring rozszerzone o `dispatcher_unavailable: 'Dispatcher niedostępny'`. **Testy**: 3 nowe cases w `test/unit/alert-dispatcher.spec.ts` (helper shape, traceId undefined bez przekazania, traceId propagacja) + rozszerzenie "all known reasons" w `test/unit/summary-scheduler.spec.ts` (7 → 8 reasons z `dispatcher_unavailable` → `Dispatcher niedostępny`). 325/325 unit pass, `tsc --noEmit` clean. **Odrzucone**: remove @Optional() z dispatcher DI (opcja A — Sprint 19 jeśli w ogóle, ujemny ROI); pełny refactor signature `dispatch()` żeby zawsze wymuszać traceId (wymagałby rozszerzenie `handleBankruptcy`/`triggerCorrelatedAlert`/OptionsFlow signatures — scope creep).
- TASK-12 report_generator render direct-comparison sub_groups (23.04.2026): fixed. V5 backtest (e07bbc2 + f69cfa8) dodał 2 direct-comparison sub_groups ze schemą `n_a/n_b` zamiast standardowego `n`: `cluster_buy_vs_single_buy` (H1) i `hc_vs_ctrl_direct` (H6). `generate_report` w `report_generator.py:118-125` skipował je silent przez `sub_n = sub_data.get("n", 0); if sub_n == 0: continue` — markdown `backtest_report.md` nie zawierał kluczowych wniosków (HC SŁABSZY niż control SELL d=-0.14 30d p=0.0162 ✓; cluster BUY vs solo BUY p>0.37 wszystkie horyzonty). **Fix**: 3 nowe helpery w `report_generator.py` — `_direct_comparison_meta(sub_data)` (schema detection przez presence `n_cluster+n_single` lub `n_hc+n_ctrl`), `_direct_comparison_sig(h)` (hierarchy: strict Bonferroni → Bonferroni → 005 → ✗), `_direct_comparison_table(sub_data, meta)` (tabela 8-kolumnowa z formatem p-value `:.4f` w renderer bo analyzer linia 486 zapisuje raw `float(p_value)` bez `round(4)`). Main loop w `generate_report` — guard PRZED `sub_n == 0 skip`: `direct_meta = _direct_comparison_meta(sub_data); if direct_meta is not None: render via _direct_comparison_table`. Standardowy `n`-path bez zmian. `from __future__ import annotations` dodane (Python 3.8 compat dla `dict | None` type hints). **Regenerate**: `python3 -c "import json; from report_generator import generate_report, save_report; save_report(generate_report(json.load(open('data/results/backtest_results.json')), 'data/form4_transactions.csv'))"` → `backtest_report.md` linia 72 cluster_buy_vs_single_buy, linia 346 hc_vs_ctrl_direct. 13 nowych testów w `scripts/backtest/tests/test_report_generator.py` (meta detection, sig hierarchy, table rendering, note row graceful, integration real JSON, fallback gdy brak direct-comparison). **Odrzucone**: fix p_value rounding w analyzer.py:486 (wymagałoby regenerate JSON, rozszerzenie scope), rendering `_note` informational blurbs w H5/H6 (orthogonal do direct-comparison), integration test "każdy JSON sub_group ma section w markdown" (zbyt ciężki guard dla 1h task), regenerate V5 JSON przez `run_backtest.py` (report rendering only scope). Python testy total: 28 (15 analyzer + 13 report_generator).

## System totals (23.04.2026, po TASK-12 — Sprint 18 complete)

42 tickery aktywne (28 healthcare + 14 semi observation) + 9 soft-deleted, **29 REST endpoints**, **12 TypeORM entities**, **7 BullMQ queues**, **8 active alert rules**, 12 disabled rules, **20 @Logged methods** (19 baseline + PdufaBioService.runCollectionCycle override — parsed count visibility). **Form4Pipeline decision tree (8 kroków)** — kroki 3/4/7 używają identycznej `isCsuiteRole()` whitelist (TASK-10 ujednolicenie 23.04.2026): pure Director SELL → `SKIP_DIRECTOR_SELL` (Sprint 15 anty-sygnał), non-role SELL → `SKIP_NON_ROLE_SELL` (TASK-02, 22.04.2026), C-suite SELL → `ALERT_DB_ONLY_CSUITE_SELL_NO_EDGE` observation (Sprint 16b), reszta SELL → `ALERT_DB_ONLY_SELL_NO_EDGE`. Backtest V5 (f69cfa8): 128 testów (threshold p<0.000391), 19 Bonferroni ✓, healthcare SELL zero edge, control SELL d=+0.10 30d ✓✓✓, cluster vs solo BUY p>0.37 (d [-0.23,+0.22] — TASK-11 verified not None). `backtest_report.md` renderuje wszystkie sub_groups w tym direct-comparison cluster_buy_vs_single_buy + hc_vs_ctrl_direct (TASK-12 23.04.2026). Warn-level actions: `ALERT_TELEGRAM_FAILED`, `REDIS_ERROR`, `PARSER_EMPTY` (TASK-06). AlertDeliveryGate: hardcoded `MAX_TELEGRAM_ALERTS_PER_SYMBOL_PER_DAY=5` per-ticker (TASK-07 verified), raport 8h pokazuje breakdown nonDeliveryReason z PL etykietami. **CorrelationService patterns**: INSIDER_CLUSTER **SELL only** od TASK-09 23.04.2026 (BUY disabled — V5 cluster vs solo p>0.37, solo BUY rule d=+0.75-0.92 pokrywa); INSIDER_PLUS_8K + INSIDER_PLUS_OPTIONS bez zmian. **Unit tests**: 326 TypeScript w 13 suites (+3 helper FOLLOW-1, +1 ASX regression FOLLOW-6) + 28 Python w `scripts/backtest/tests/` (15 analyzer TASK-11 + 13 report_generator TASK-12). **Sprint 18 complete** — 12/12 tasków done (TASK-01..12, 22-23.04.2026) + FOLLOW-1..6 (23.04.2026): dispatcher_unavailable helper+label, SecFilingAnalysis typed column (zero `as any` w gptAnalysis persistence), frontend TimelineAlert + PriorityChip typed nonDeliveryReason, Vite Tailscale allowedHosts, ASX regression integration test guard.